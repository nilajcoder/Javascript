


/*

In JavaScript modules, export means:

Make a variable, function, class, or value available for use in another file.

By default, everything inside a JS file (module) is private.
export makes it public.
*/

export function add(a,b){

    return a+b
}

export function sub(a,b){

    return a-b
}